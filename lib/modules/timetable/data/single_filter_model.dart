class SingleFilterModel {
  String identifier;
  String label;
  bool isSelected;

  SingleFilterModel(
      {required this.identifier,
      required this.label,
      required this.isSelected});
}
